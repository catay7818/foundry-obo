info:
  name: Seed Data
  type: http
  seq: 1

http:
  method: POST
  url: "{{functionHost}}/api/seed"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
