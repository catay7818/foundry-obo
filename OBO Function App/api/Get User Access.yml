info:
  name: Get User Access
  type: http
  seq: 2

http:
  method: GET
  url: "{{functionHost}}/api/user/access"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
