info:
  name: Chat Response
  type: http
  seq: 1

http:
  method: POST
  url: "{{agentHost}}/responses"
  body:
    type: json
    data: |-
      {
        "input": "show me sales data",
        "stream": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
