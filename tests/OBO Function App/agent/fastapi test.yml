info:
  name: fastapi test
  type: http
  seq: 4

http:
  method: POST
  url: http://127.0.0.1:8000/query
  body:
    type: json
    data: |-
      {
        "container": "Sales"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
